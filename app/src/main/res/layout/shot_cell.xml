<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/shot_image"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/material_card_media_area_framed_padding_horizontal"
            android:layout_marginVertical="@dimen/material_card_media_area_framed_padding_vertical"
            android:contentDescription="@string/image_shot"
            android:scaleType="centerCrop"
            tools:src="@drawable/shot_placeholder" />

        <ImageView
            android:id="@+id/view_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/shot_image"
            android:layout_marginEnd="64dp"
            android:layout_marginRight="64dp"
            android:layout_toLeftOf="@+id/like_icon"
            android:layout_toStartOf="@+id/like_icon"
            app:srcCompat="@drawable/ic_visibility_black_24dp" />

        <ImageView
            android:id="@+id/like_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignEnd="@+id/shot_image"
            android:layout_alignRight="@+id/shot_image"
            android:layout_below="@+id/shot_image"
            android:layout_marginEnd="64dp"
            android:layout_marginRight="64dp"
            app:srcCompat="@drawable/ic_favorite_black_24dp" />

    </RelativeLayout>

</android.support.v7.widget.CardView>